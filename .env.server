# PostgreSQL connection — update with your credentials
DATABASE_URL=postgresql://timekeeper_user:yourpassword@localhost:5432/broadcast_timekeeper

# Wasp JWT secret — generate with: openssl rand -hex 32
JWT_SECRET=replace_with_32_byte_hex_secret
